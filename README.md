# Julia-Fractal-Generator
A julia fractal generator, created using python. UI designed through tkinter, Calculations done using numpy, Fractal shown using matplotlib.

Finding -> When Julia fractal is associated with a number inside the Mandelbrot set, it generates a connected fractal, otherwise it generates a disconnected one. Interesting fractals are generated when values are chosen near the boundary of the Mandelbrot set.
